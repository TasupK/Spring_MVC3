<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.freeboard.mapper.BoardSQLMapper">

	<select id="selectBoard" resultType="com.java.freeboard.vo.BoardVO">
		SELECT 
			fb.board_no
			,fm.member_name
			,fb.board_title
			,fb.board_content
			,fb.reg_date
		FROM fb_board as fb, fb_member fm
		WHERE fb.member_no = fm.member_no 
		ORDER BY board_no DESC
	</select>
	
	<select id="getBoard" resultType="com.java.freeboard.vo.BoardVO" parameterType="com.java.freeboard.vo.BoardVO">
		SELECT 
			board_no
			,member_no
			,board_title
			,board_content
			,reg_date
			,mod_id
			,mod_date
		FROM fb_board
		WHERE 
			board_no = #{board_no}
	</select>
	
	<insert id="insertBoard" parameterType="com.java.freeboard.vo.BoardVO">
		INSERT INTO fb_board(board_title, board_content, member_no)
		VALUES (#{board_title}, #{board_content}, #{member_no});
	</insert>
	
	
</mapper>